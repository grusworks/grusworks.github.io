name: Ping Supabase

on:
  schedule:
    - cron: '0 12 */3 * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl -s https://lscwnnvmrzpctdhsksaf.supabase.co/rest/v1/ -o /dev/null
